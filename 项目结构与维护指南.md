# 项目结构与维护指南

## 1. 文档目标

这份文档面向后续维护者，目标不是介绍“怎么使用 Noitool”，而是回答下面几类维护问题：

- 这个项目的整体结构是什么，哪些目录是源码，哪些目录是游戏解包数据。
- 浏览器端、Node 端、CLI Worker、Docker Worker 各自负责什么。
- 种子信息查看、种子搜索、本地多线程、集群协作分别走哪条代码路径。
- 噩梦模式相关逻辑分布在哪里，修改时要注意什么。
- 版本升级、规则扩展、Provider 扩展、数据库升级、构建脚本调整时应该改哪些文件。

这份文档基于当前仓库状态整理，当前主版本见 [package.json](/f:/noita-tools/package.json) 中的 `version`，目前为 `35.0.1`。

## 2. 项目定位

项目本质上是一个围绕 Noita 种子计算的前端工具站，核心计算逻辑大量复刻了游戏/Mod 逻辑，并通过 WASM 与 Worker 提高性能。

当前功能可以分成 4 类：

- 种子信息查看：输入单个种子，生成圣山、商店、法杖、炼金、天气、真菌转换等信息。
- 种子搜索：按照规则批量搜索满足条件的种子。
- 本地并行搜索：浏览器 Web Worker 或 Node worker_threads 并行计算。
- 集群协作搜索：一台“主站/Host”分发搜索块，多台浏览器节点或 CLI Worker 回传结果。

## 3. 顶层目录职责

### 3.1 源码与配置

- [src](/f:/noita-tools/src)：
  前端源码、搜索引擎、SeedInfo 计算核心、随机数/WASM 接口、浏览器/Node Worker 入口都在这里。
- [server](/f:/noita-tools/server)：
  轻量 Node 服务端。主要职责不是“计算”，而是提供静态文件与 Socket.IO 任务中转。
- [public](/f:/noita-tools/public)：
  浏览器静态资源，包括图标、OCR 模型、本地化文本、主题 CSS。
- [scripts](/f:/noita-tools/scripts)：
  打包与部署脚本，分别覆盖 Node 部署、nginx 部署、Docker 部署、Worker 打包。
- [.github/workflows](/f:/noita-tools/.github/workflows)：
  GitHub Actions 构建配置。目前只有 `build.yml`。

### 3.2 运行产物与打包

- [build](/f:/noita-tools/build)：
  Vite 前端构建产物。
- [console-build](/f:/noita-tools/console-build)：
  `consoleBuild.cjs` 生成的 Node CLI / Worker 独立运行产物。

### 3.3 游戏数据与 Mod 数据

- [data](/f:/noita-tools/data)：
  游戏解包数据，来自 Noita `data.wak`。
- [mods](/f:/noita-tools/mods)：
  游戏 Mod 数据，当前噩梦模式依赖 `mods/nightmare`。
- [data.zip](/f:/noita-tools/data.zip)、[mods.zip](/f:/noita-tools/mods.zip)：
  数据压缩包，通常用于分发或备份，不是运行时直接读取入口。

### 3.4 关键根文件

- [package.json](/f:/noita-tools/package.json)：
  主工程依赖、脚本、版本号、Node 版本要求。
- [search.package.json](/f:/noita-tools/search.package.json)：
  `console-build` 产物使用的独立 package 模板。
- [vite.config.ts](/f:/noita-tools/vite.config.ts)：
  浏览器构建配置、代理配置、Worker 配置。
- [consoleBuild.cjs](/f:/noita-tools/consoleBuild.cjs)：
  Node 搜索/Worker 独立打包脚本。
- [Dockerfile](/f:/noita-tools/Dockerfile)：
  主站镜像。
- [Dockerfile.worker](/f:/noita-tools/Dockerfile.worker)：
  计算节点镜像。

## 4. 运行形态总览

### 4.1 浏览器前端

入口是 [src/index.tsx](/f:/noita-tools/src/index.tsx)，只做三件事：

- 加载全局样式与 i18n。
- 打印 `APP_VERSION`。
- 挂载 `<App />`。

应用主壳在 [src/components/App.tsx](/f:/noita-tools/src/components/App.tsx)：

- 使用 `HashRouter`，所以部署到子路径时不需要服务端做复杂的 SPA 路由回退规则。
- 提供主题、炼金配置、设置弹窗、DB 错误处理、WASM 可用性检查。
- 真实业务页由 [src/components/Body.tsx](/f:/noita-tools/src/components/Body.tsx) 决定。

### 4.2 Node 服务端

入口是 [server/standalone.mjs](/f:/noita-tools/server/standalone.mjs)。

职责很有限：

- 读取 `package.json`，把版本写入 `process.env.npm_package_version`。
- 提供 `/api/cluster_stats` 和 `/api/session`。
- 托管 `build/` 目录静态文件。
- 建立 Socket.IO 服务。
- 把所有计算池相关 socket 事件交给 [server/io/compute.mjs](/f:/noita-tools/server/io/compute.mjs)。

它不是搜索计算核心，真正的搜索仍然在浏览器 Worker 或 CLI Worker 中执行。

### 4.3 CLI / Docker Worker

入口是 [src/consoleSearch.ts](/f:/noita-tools/src/consoleSearch.ts)。

职责：

- 读取 CLI 参数或 `NOITOOL_*` 环境变量。
- 创建 Node 版 `SeedSolver`。
- 连接主站 Socket.IO。
- 拉取任务块并计算。
- 回传结果。

`console-build` 打包后，这套逻辑被用于：

- 本地命令行计算。
- Docker Worker 计算节点。

## 5. 页面与功能模块结构

### 5.1 主要页面

`Body.tsx` 当前有 3 个主 Tab：

- `/info`：种子信息查看，对应 [src/components/SeedInfo](/f:/noita-tools/src/components/SeedInfo)
- `/search`：种子搜索，对应 [src/components/SearchSeeds](/f:/noita-tools/src/components/SearchSeeds)
- `/compute`：计算池节点页面，对应 [src/components/Compute](/f:/noita-tools/src/components/Compute)

仓库里还存在但不一定在主导航暴露的功能：

- [src/components/LiveSeedStats](/f:/noita-tools/src/components/LiveSeedStats)：OCR / 实时种子读取。
- [src/components/TestBench](/f:/noita-tools/src/components/TestBench)：实验或调试页。
- [src/components/Profile](/f:/noita-tools/src/components/Profile)：用户/Patreon 相关。
- [src/components/Settings](/f:/noita-tools/src/components/Settings)：本地配置中心。

### 5.2 种子信息查看模块

入口使用 [src/components/SeedInfo/SeedDataOutput.tsx](/f:/noita-tools/src/components/SeedInfo/SeedDataOutput.tsx)。

它负责：

- 初始化 `GameInfoProvider`。
- 从 Dexie 读取并恢复单种子配置。
- 调用 `provideAll()` 生成整包种子信息。
- 把 `gameInfoProvider + data` 注入 `GameInfoContext`。

[src/components/SeedInfo/SeedInfo.tsx](/f:/noita-tools/src/components/SeedInfo/SeedInfo.tsx) 只是装配层，把结果拆给各个 View：

- `HolyMountain`
- `Start`
- `Weather`
- `Watercave`
- `Alchemy`
- `Biome`
- `FungalShifts`
- `Map`
- `ExcavationsiteCubeChamber`
- `SnowcaveSecretChamber`

因此新增“显示型数据面板”时，通常不是去改 `SeedInfo.tsx` 里的算法，而是：

1. 在 `GameInfoProvider.provideAll()` 中产出数据。
2. 新增一个 `SeedInfoViews/*.tsx` 组件。
3. 再把它挂到 `SeedInfo.tsx`。

### 5.3 搜索模块

入口在 [src/components/SearchSeeds/SearchSeeds.tsx](/f:/noita-tools/src/components/SearchSeeds/SearchSeeds.tsx)。

它会先创建一个 `useGameInfoProvider("0")` 的上下文，供规则编辑界面读取法术、材料、天赋等参考数据，然后再挂载 `SearchContextProvider`。

搜索页主要分成两块：

- `RuleList`：规则树展示/切换。
- `RuleConstructor`：具体规则编辑器。

对应目录：

- [src/components/SearchSeeds/RuleList.tsx](/f:/noita-tools/src/components/SearchSeeds/RuleList.tsx)
- [src/components/SearchSeeds/RuleConstructor.tsx](/f:/noita-tools/src/components/SearchSeeds/RuleConstructor.tsx)
- [src/components/SearchSeeds/SearchViews](/f:/noita-tools/src/components/SearchSeeds/SearchViews)

规则树状态由：

- [src/components/SearchSeeds/ruleReducer.tsx](/f:/noita-tools/src/components/SearchSeeds/ruleReducer.tsx)
- [src/components/SearchSeeds/ruleNormalization.ts](/f:/noita-tools/src/components/SearchSeeds/ruleNormalization.ts)
- [src/components/SearchSeeds/node.ts](/f:/noita-tools/src/components/SearchSeeds/node.ts)

共同维护。

## 6. 搜索运行链路

### 6.1 搜索状态中心

[src/components/SearchSeeds/SearchContext.tsx](/f:/noita-tools/src/components/SearchSeeds/SearchContext.tsx) 是搜索页的总调度器，负责：

- 从 Dexie 读取当前搜索定义。
- 管理规则树。
- 管理 `ChunkProvider`。
- 管理浏览器本地 `SeedSolver`。
- 管理本地多线程开关。
- 管理集群连接与状态。
- 生成 `computeJobHash`，作为当前搜索任务的隔离键。

当前 `computeJobHash` 由以下内容计算得到：

- `from`
- `to`
- `maxResults`
- `isNightmare`
- `rules`
- `customSeedList`

这意味着只要这些条件变化，就会被视作新的搜索任务。

### 6.2 本地搜索

本地搜索走 [src/services/compute/CallbackComputeHandler.ts](/f:/noita-tools/src/services/compute/CallbackComputeHandler.ts)。

流程是：

1. `ChunkProvider` 生成搜索块。
2. `SeedSolver.searchChunk()` 把块拆给多个 Worker。
3. Worker 内部运行 `SeedSearcher.findSync()`。
4. 结果回写 `ChunkProvider.commitChunk()`。

### 6.3 集群协作搜索

浏览器作为 Host 时，会额外挂载 [src/services/compute/SocketComputeProvider.ts](/f:/noita-tools/src/services/compute/SocketComputeProvider.ts)。

它做两件事：

- 对外提供任务块：响应 `compute:get_job`
- 接收远端 Worker 回传的结果：处理 `compute:done`

集群关键点：

- 任务块依旧由本地 `ChunkProvider` 分配。
- 远端 Worker 只是额外算力来源。
- 本地 `CallbackComputeHandler` 与远端 `SocketComputeProvider` 可以同时工作。

### 6.4 Job 隔离

为了避免旧任务结果混入新任务，当前有两层保护：

- `SearchContext.tsx` 生成基于实际搜索配置的 `computeJobHash`
- `SocketComputeProvider.handleResults()` 检查返回的 `jobName`，不匹配直接丢弃

这部分是集群一致性维护的关键，不要轻易回退。

## 7. Worker 与并行计算结构

### 7.1 浏览器 Worker

[src/services/seedSolverHandler.ts](/f:/noita-tools/src/services/seedSolverHandler.ts) 创建浏览器端 Worker：

- 入口文件是 [src/workers/seedSearcher.worker.ts](/f:/noita-tools/src/workers/seedSearcher.worker.ts)
- 使用 `comlink` 暴露 `SeedSearcher`

### 7.2 Node Worker

[src/services/seedSolverHandler.node.ts](/f:/noita-tools/src/services/seedSolverHandler.node.ts) 创建 Node 版 Worker：

- 入口文件是 [src/workers/seedSearcher.worker.node.ts](/f:/noita-tools/src/workers/seedSearcher.worker.node.ts)
- 使用 `worker_threads + comlink/dist/umd/node-adapter.js`

### 7.3 分块策略

`SeedSolver.searchChunk()` 的行为：

- 没有地图规则时，按 worker 数平均拆块。
- 有地图规则时，单块大小会进一步收缩到最大 100，用于降低地图生成造成的内存/GC 压力。

这说明“地图相关规则”在性能上明显比普通规则更重。

## 8. SeedSearcher 与规则执行

[src/services/seedSearcher.ts](/f:/noita-tools/src/services/seedSearcher.ts) 是“批量判断一个 seed 是否满足规则”的执行器。

核心职责：

- 对规则按成本排序，尽量先判断便宜规则。
- 为当前 seed 设置世界随机种子。
- 递归执行逻辑规则 `and/or/not`。
- 对具体规则调用 `gameInfoProvider.providers[rule.type].test(rule)`。

因此，扩展新规则时，最终必须落到一个新的 Provider `test()` 能力，或者复用现有 Provider。

### 8.1 规则结构

规则类型定义在 [src/services/SeedInfo/infoHandler/IRule.ts](/f:/noita-tools/src/services/SeedInfo/infoHandler/IRule.ts)：

- 逻辑节点：`and / or / not`
- 规则节点：`IRule`

UI 构造的是规则树，执行时也是沿着这棵树判断。

## 9. GameInfoProvider 与 Provider 体系

### 9.1 总入口

[src/services/SeedInfo/infoHandler/index.ts](/f:/noita-tools/src/services/SeedInfo/infoHandler/index.ts) 中的 `GameInfoProvider` 是整个种子信息系统的总入口。

它负责：

- 加载随机数模块 `loadRandom()`
- 动态导入各个 `InfoProvider`
- 根据依赖顺序构造 Provider
- 在 `provideAll()` 中产出页面展示所需的整包数据
- 统一把模式配置（普通/噩梦）同步到各 Provider

### 9.2 Provider 目录

目录在 [src/services/SeedInfo/infoHandler/InfoProviders](/f:/noita-tools/src/services/SeedInfo/infoHandler/InfoProviders)。

当前主要 Provider 包括：

- `Alchemy`
- `AlwaysCast`
- `Biome`
- `BiomeModifier`
- `ChestRandom`
- `EntranceWand`
- `Lottery`
- `Map`
- `Material`
- `PacifistChest`
- `Perk`
- `Potion / PotionSecret / PotionRandomMaterial`
- `PowderStash`
- `Shop`
- `Spell`
- `StartingBomb / StartingFlask / StartingSpell`
- `Wand`
- `WaterCave`
- `Weather`

附带有较多 `*.spec.ts` / `*.spec.tsx`，说明这里是最值得写测试的区域。

### 9.3 Provider 构造依赖

`GameInfoProvider.buildInfoProviders()` 内部通过 `getDependencies()` 解决 Provider 构造顺序。

已显式编码的依赖包括：

- `shop` 依赖 `wand + spells`
- `pacifistChest` 依赖 `chestRandom`
- `chestRandom` 依赖 `spells`
- `entranceWand` 依赖 `wand`
- `map` 额外接收 `isNightmare`

如果新增 Provider 需要依赖别的 Provider，不要只改构造函数，还要同步改 `getDependencies()`。

## 10. 随机数、WASM 与图像处理

### 10.1 随机数总入口

入口是 [src/services/SeedInfo/random/index.ts](/f:/noita-tools/src/services/SeedInfo/random/index.ts)。

它会同时加载两套 WASM：

- `src/services/SeedInfo/noita_random/noita_random.wasm`
- `src/services/SeedInfo/wasm/rng.wasm`

然后交给 [src/services/SeedInfo/random/random.ts](/f:/noita-tools/src/services/SeedInfo/random/random.ts) 做 JS 包装，生成统一的 `IRandom` 接口。

### 10.2 random.ts 的作用

`random.ts` 的职责不是“简单透传 wasm”，而是补了很多游戏级逻辑：

- 法术概率表读取
- 已解锁法术过滤
- `GetRandomAction`
- `GetRandomActionWithType`
- 随机表加权选择
- Map 相关辅助对象与内存分配

也就是说：

- 如果游戏升级导致法术池概率变化，通常要看这里。
- 如果解锁法术逻辑变化，也要看这里。

### 10.3 图像处理

[src/services/imageActions/index.ts](/f:/noita-tools/src/services/imageActions/index.ts) 负责在不同运行环境下加载不同图像实现：

- 浏览器端：`webImageActions` 或 `webCanvasImageActions`
- Node 端：`nodeImageActions`

地图 Provider 高度依赖这层抽象。

### 10.4 地图相关特殊风险

地图系统涉及：

- 大图/分块读取
- `@napi-rs/canvas`
- `MapHandler`
- 噩梦世界图

这里是最容易出现“浏览器正常、Node/console-build 崩溃”问题的区域。此前已经发生过：

- `console-build` 中 wasm 路径缺失
- Node 图像动作导入路径不正确
- 噩梦世界图 data URI 损坏导致 Node 原生图像解码崩溃

因此地图层改动后，不要只测浏览器页面，也要测 `console-build`。

## 11. 噩梦模式相关结构

### 11.1 数据来源

噩梦模式不是普通模式的一个简单开关，而是明确依赖 `mods/nightmare`。

已知：

- 地形不同
- 圣山层数不同
- 圣山坐标不同
- 入口法杖存在于噩梦模式，不存在于普通模式
- 商店、和平主义宝箱等部分生成逻辑需要按噩梦圣山位置重算

### 11.2 主要分布位置

噩梦模式代码主要散落在这些位置：

- `GameInfoProvider.applyConfigToProviders()`
- 各个 `InfoProvider` 的 `setNightmareMode()`
- `holyMountainLocations.ts`
- `Map` Provider
- `Perk`/`Shop`/`PacifistChest`/`Lottery`/`AlwaysCast`
- 搜索页规则标准化与展示逻辑

### 11.3 维护原则

凡是“与圣山层数、圣山坐标、入口点、商店位置、和平主义宝箱位置、噩梦入口法杖”相关的改动，优先检查：

- 普通模式与噩梦模式是否共用一套位置函数
- 页面显示层和搜索判定层是否使用同一套位置来源
- 浏览器模式和 `console-build` 是否一致

## 12. 计算池服务端结构

[server/io/compute.mjs](/f:/noita-tools/server/io/compute.mjs) 是集群协议核心。

它维护：

- `counts.hosts`
- `counts.workers`
- `counts.appetite`
- socket 注册表
- `pendingJobs`

主要事件：

- `compute:host:register`
- `compute:worker:register`
- `compute:need_job`
- `compute:get_job`
- `compute:done`

### 12.1 版本兼容策略

当前服务端已经改为“完整版本号一致才允许注册”，而不是只比较主版本。

也就是说，`35.0.1` 与 `35.0.0` 现在都会被视为不兼容。

这是为了避免：

- 同主版本但逻辑已变更的旧 Worker
- 老任务实现回传结果混入新搜索

### 12.2 结果隔离策略

当前回传结果会带 `jobName`，Host 收到不属于当前任务的结果时会直接忽略。

这部分是协议层的重要安全垫。

## 13. 浏览器本地存储与数据库

[src/services/db.ts](/f:/noita-tools/src/services/db.ts) 使用 Dexie 管理浏览器 IndexedDB。

主要表：

- `configItems`
- `seedInfo`
- `favorites`
- `searches`

主要用途：

- 保存设置项
- 保存单种子配置
- 保存收藏
- 保存搜索任务定义

### 13.1 数据库升级方式

当前 DB 版本已经迭代到 `11`。

升级模式是：

- 在 `new NoitaDB()` 内继续追加 `this.version(x)` 块
- 在 `.upgrade()` 中写迁移逻辑

不要修改旧版本迁移块的语义，新增迁移时只往后加。

## 14. 构建与打包结构

### 14.1 浏览器构建

[vite.config.ts](/f:/noita-tools/vite.config.ts) 关键点：

- `base: "./"`：支持相对路径部署
- `APP_VERSION` 来自 `package.json`
- `/socket.io` 和 `/api` 代理到 `localhost:3001`
- `vite-plugin-comlink` 用于 Worker 通讯
- `build.outDir = "./build"`

### 14.2 console-build

[consoleBuild.cjs](/f:/noita-tools/consoleBuild.cjs) 负责打包 Node CLI / Docker Worker。

当前它除了 esbuild 打包，还额外承担这些“补丁式修正”：

- 修正 worker 入口从 `.ts` 指向 `.js`
- 修正 Node 端 `nodeImageActions` 导入路径
- 复制 `noita_random.wasm`
- 复制 `rng.wasm`
- 复制 `Alchemy.wasm`
- 复制 `FungalShift.wasm`
- 生成 `console-build/package.json`

因此它不是“纯编译脚本”，而是包含运行时修复的关键文件。

### 14.3 Docker 主站

[Dockerfile](/f:/noita-tools/Dockerfile) 采用两阶段：

- builder：安装完整依赖并执行 `npm run build`
- production：只保留生产依赖与 `build/`、`server/`

### 14.4 Docker Worker

[Dockerfile.worker](/f:/noita-tools/Dockerfile.worker) 同样两阶段：

- builder：执行 `npm run console-build`
- production：复制 `console-build/` 并直接运行 `consoleSearch.js`

### 14.5 GitHub Actions

[.github/workflows/build.yml](/f:/noita-tools/.github/workflows/build.yml) 当前职责：

- `npm ci --legacy-peer-deps`
- `tsc --noEmit`
- `npm run build`
- `npm run console-build`
- `bash ./scripts/pack-node.sh`
- `bash ./scripts/pack-nginx.sh`
- 上传 `console-build/` 产物

注意：这里已经显式使用 `bash ./scripts/*.sh`，避免脚本执行位问题。

## 15. scripts 目录职责

[scripts](/f:/noita-tools/scripts) 目录用于离线部署/打包。

主要脚本：

- `pack-node.*`
- `pack-nginx.*`
- `pack-docker.sh`
- `pack-worker.sh`
- `deploy-node.*`
- `deploy-nginx.*`
- `deploy-docker.*`
- `install-node.sh`

维护时要注意：

- Windows 与 Linux 脚本不完全对等
- GitHub Actions 主要依赖 `.sh`
- 部署脚本与 Dockerfile、`build/`、`console-build/` 的输出结构强相关

## 16. 测试结构

项目现有测试主要集中在两类：

- `src/services/SeedInfo/infoHandler/InfoProviders/*.spec.ts`
- `src/services/seedSearcher.spec.ts`

说明当前最值得回归验证的区域是：

- 具体生成逻辑
- 搜索规则匹配逻辑
- 随机数与地图生成相关逻辑

如果后续改动的是 UI 文本、简单布局，未必必须补测试。
如果改动的是生成逻辑、规则匹配、噩梦模式、圣山布局、集群协议，建议优先补或修改测试。

## 17. 典型维护场景与改动清单

### 17.1 只升级版本号

最少需要检查这些位置：

- [package.json](/f:/noita-tools/package.json)：主版本号
- [Dockerfile](/f:/noita-tools/Dockerfile)：当前硬编码了 `ENV npm_package_version=35.0.1`
- `APP_VERSION` 注入链路：`vite.config.ts`
- `console-build/package.json` 生成链路：`consoleBuild.cjs` 会从主 `package.json` 同步
- 兼容性校验：`server/io/compute.mjs` 按完整版本比较

如果只改 `package.json` 不改 `Dockerfile`，容器里主站版本环境变量会滞后。

### 17.2 升级 Node 版本

需要同步检查：

- `package.json.engines.node`
- `Dockerfile`
- `Dockerfile.worker`
- `consoleBuild.cjs` 中 esbuild `target`
- GitHub Actions `setup-node`

### 17.3 新增一个 SeedInfo Provider

至少要改：

1. `src/services/SeedInfo/infoHandler/InfoProviders/` 下新增 Provider
2. `GameInfoProvider` 的 `IProviders` 类型
3. `buildInfoProviders()` 的 `importMap`
4. 如果有依赖，再改 `getDependencies()`
5. 如果要显示在页面上，再改 `provideAll()` 与 `SeedInfoViews`
6. 如果要支持搜索，还要加 `test(rule)` 并接入搜索 UI

### 17.4 新增一个搜索规则

通常要改这些层：

1. 搜索 UI：
   `src/components/SearchSeeds/SearchViews/`
2. 规则树构造/默认值：
   `ruleReducer.tsx`、`ruleNormalization.ts`
3. 规则执行：
   某个 `InfoProvider.test(rule)`
4. 排序权重：
   `src/services/seedSearcher.ts` 的 `searchWeights`
5. 如需单种子详情复用，也要检查 `SeedInfoViews`

### 17.5 修改噩梦模式生成逻辑

优先排查这些点：

- `mods/nightmare` 是否已有真实来源
- `holyMountainLocations.ts`
- `Shop.ts`
- `PacifistChest.ts`
- `Lottery.ts`
- `AlwaysCast.ts`
- `Perk.ts`
- `Map` Provider
- `GameInfoProvider.provideAll()`
- 搜索详情页的圣山层数展示
- 搜索规则匹配逻辑是否仍然与详情页一致

### 17.6 修改圣山层数或坐标

需要同时改“展示”和“计算”：

- `holyMountainLocations.ts`
- SeedInfo 详情页展示组件
- 商店 Provider
- 天赋 Provider
- 和平主义宝箱 Provider
- 任何按层级遍历圣山数组的搜索 `test()`

维护原则：

- 不要在多个 Provider 内各自硬编码层数和坐标
- 优先收敛到共享位置函数

### 17.7 修改集群协议

至少检查：

- `server/io/compute.mjs`
- `src/services/compute/ComputeSocket.ts`
- `src/services/compute/SocketComputeProvider.ts`
- `src/components/SearchSeeds/SearchContext.tsx`
- `src/consoleSearch.ts`

协议字段变更时要特别注意：

- Host 发出的 job 数据结构
- Worker 回传结构
- `jobName` 是否保留
- 版本不一致的拒绝逻辑是否同步

### 17.8 修改 console-build

优先检查：

- `consoleBuild.cjs`
- `search.package.json`
- `src/consoleSearch.ts`
- `src/services/seedSolverHandler.node.ts`
- `src/workers/seedSearcher.worker.node.ts`

然后必须做两种验证：

1. `node consoleBuild.cjs`
2. 直接运行 `console-build/workers/seedSearcher.worker.node.js`

只看浏览器页面是否正常是不够的。

### 17.9 修改随机数/WASM 层

至少关注：

- `src/services/SeedInfo/random/index.ts`
- `src/services/SeedInfo/random/random.ts`
- `src/services/SeedInfo/random/rngWasm.ts`
- `src/services/SeedInfo/noita_random/*`
- `src/services/SeedInfo/wasm/*`

这类改动风险很高，容易影响：

- 单种子详情
- 搜索命中结果
- 浏览器 Worker
- Node Worker

### 17.10 修改数据库结构

只在 `db.ts` 中追加新版本迁移，不要回写旧版本逻辑。

同时要检查：

- 默认搜索创建逻辑
- 本地设置默认值
- 导入导出兼容性

### 17.11 更新游戏数据或 Mod 数据

主要关注：

- `data/`
- `mods/`
- `src/services/SeedInfo/data/`

如果只是替换原始解包资源，不一定自动影响运行逻辑，因为很多运行时依赖的是已整理过的 JSON / TS 数据。

因此更新游戏版本后要区分两层：

- 原始素材是否更新
- 项目内部引用的数据快照是否更新

## 18. 建议的维护顺序

以后做较大改动时，建议按下面顺序推进：

1. 先定位改动属于哪一层：UI、Provider、随机数、地图、搜索、协议、构建。
2. 找共享入口，不要直接在多个组件/Provider 里复制修补。
3. 先让浏览器本地搜索正确，再验证集群和 console-build。
4. 涉及噩梦模式时，同时验证普通模式与噩梦模式。
5. 涉及圣山/商店/宝箱时，同时验证详情页与搜索规则匹配。
6. 涉及 Node/Worker 路径时，必须额外验证 `console-build`。

## 19. 建议优先阅读顺序

新维护者如果要快速建立全局理解，建议按这个顺序读：

1. [package.json](/f:/noita-tools/package.json)
2. [src/components/App.tsx](/f:/noita-tools/src/components/App.tsx)
3. [src/components/Body.tsx](/f:/noita-tools/src/components/Body.tsx)
4. [src/components/SeedInfo/SeedDataOutput.tsx](/f:/noita-tools/src/components/SeedInfo/SeedDataOutput.tsx)
5. [src/components/SearchSeeds/SearchContext.tsx](/f:/noita-tools/src/components/SearchSeeds/SearchContext.tsx)
6. [src/services/seedSearcher.ts](/f:/noita-tools/src/services/seedSearcher.ts)
7. [src/services/SeedInfo/infoHandler/index.ts](/f:/noita-tools/src/services/SeedInfo/infoHandler/index.ts)
8. [src/services/SeedInfo/random/random.ts](/f:/noita-tools/src/services/SeedInfo/random/random.ts)
9. [server/io/compute.mjs](/f:/noita-tools/server/io/compute.mjs)
10. [consoleBuild.cjs](/f:/noita-tools/consoleBuild.cjs)

## 20. 总结

这个项目不是“一个普通 React 前端”，而是三套运行时并存：

- 浏览器前端
- Node 服务端
- Node CLI / Docker Worker

同时它也不是“只读游戏数据”的展示工具，而是包含了大量 Noita 与 Mod 逻辑复现的计算工程。后续维护时，最容易出问题的点通常不是页面，而是下面这些交叉地带：

- 普通模式与噩梦模式逻辑分叉
- 页面展示逻辑与搜索判定逻辑不一致
- 浏览器路径与 `console-build` 路径不一致
- 集群协议变更后旧 Worker 混入
- 数据更新了，但内部快照/Provider 没同步

如果把入口文件、Provider 总线、搜索执行器、随机数/WASM、集群协议和 console-build 这几块理解透，后面的维护成本会明显下降。
