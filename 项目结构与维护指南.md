# 项目结构与维护指南

## 1. 文档目标

这份文档面向后续维护者，重点回答：

- 项目整体结构是什么，哪些目录是源码，哪些目录是游戏数据
- 浏览器端、Node 服务端、CLI Worker、Docker Worker 分别负责什么
- 种子详情、搜索、本地并行、计算池协作分别走哪条代码路径
- 噩梦模式逻辑分布在哪里，改动时要注意什么
- 版本升级、规则扩展、Provider 扩展、构建脚本调整时应该改哪些文件

当前主版本见 [package.json](/f:/noita-tools/package.json)，目前为 `35.0.1`。

## 2. 项目定位

本项目不是单纯的前端展示站，而是一个包含多种运行形态的 Noita 种子计算工具：

- 浏览器端负责交互、单种子详情展示、本地搜索
- 服务端负责静态资源托管和 Socket.IO / 集群中转
- Worker 负责实际搜索计算
- `data/` 与 `mods/` 提供原版与 Mod 的可追溯数据来源

种子相关能力大致分为 4 类：

- 单种子详情查看
- 按规则批量搜索
- 本地多线程搜索
- 计算池协同搜索

## 3. 顶层目录职责

### 3.1 源码与配置

- [src](/f:/noita-tools/src)：前端源码、搜索引擎、SeedInfo 计算核心、随机数/WASM 接口、浏览器/Node Worker 入口
- [server](/f:/noita-tools/server)：轻量服务端，主要职责是静态资源托管与计算池中转
- [public](/f:/noita-tools/public)：浏览器静态资源
- [scripts](/f:/noita-tools/scripts)：打包与部署脚本
- [.github/workflows](/f:/noita-tools/.github/workflows)：CI 构建配置

### 3.2 构建产物

- [build](/f:/noita-tools/build)：Vite 前端产物
- [console-build](/f:/noita-tools/console-build)：`consoleBuild.cjs` 生成的 CLI / Worker 产物

### 3.3 游戏与 Mod 数据

- [data](/f:/noita-tools/data)：Noita 解包数据
- [mods](/f:/noita-tools/mods)：Mod 数据，当前噩梦模式依赖 `mods/nightmare`
- [data.zip](/f:/noita-tools/data.zip)、[mods.zip](/f:/noita-tools/mods.zip)：压缩包，不是运行时直接入口

### 3.4 关键根文件

- [package.json](/f:/noita-tools/package.json)：版本、脚本、依赖、Node 版本要求
- [search.package.json](/f:/noita-tools/search.package.json)：`console-build` 模板包
- [vite.config.ts](/f:/noita-tools/vite.config.ts)：前端构建、代理、Worker 配置
- [consoleBuild.cjs](/f:/noita-tools/consoleBuild.cjs)：CLI / Worker 打包逻辑
- [Dockerfile](/f:/noita-tools/Dockerfile)：主站镜像
- [Dockerfile.worker](/f:/noita-tools/Dockerfile.worker)：Worker 镜像

## 4. 运行形态总览

### 4.1 浏览器前端

入口是 [src/index.tsx](/f:/noita-tools/src/index.tsx)。

主要职责：

- 加载全局样式和 i18n
- 注入 `APP_VERSION`
- 挂载 `<App />`

应用主壳在 [src/components/App.tsx](/f:/noita-tools/src/components/App.tsx)，页面主切换在 [src/components/Body.tsx](/f:/noita-tools/src/components/Body.tsx)。

当前主要页面：

- `/info`：种子详情
- `/search`：搜索
- `/compute`：计算池节点

### 4.2 Node 服务端

入口是 [server/standalone.mjs](/f:/noita-tools/server/standalone.mjs)。

职责很有限：

- 读取 `package.json` 版本并注入 `process.env.npm_package_version`
- 提供 `/api/cluster_stats`
- 提供 `/api/session`
- 托管 `build/` 静态文件
- 建立 Socket.IO 服务
- 将计算池事件交给 [server/io/compute.mjs](/f:/noita-tools/server/io/compute.mjs)

服务端不做实际种子搜索计算。

### 4.3 CLI / Docker Worker

入口是 [src/consoleSearch.ts](/f:/noita-tools/src/consoleSearch.ts)。

职责：

- 读取 CLI 参数或 `NOITOOL_*` 环境变量
- 创建 Node 版 `SeedSolver`
- 连接主站 Socket.IO
- 拉取任务块并计算
- 回传结果

`console-build` 打包后，这套逻辑同时被 CLI 与 Docker Worker 复用。

## 5. 页面与功能模块

### 5.1 种子详情

入口在 [src/components/SeedInfo/SeedDataOutput.tsx](/f:/noita-tools/src/components/SeedInfo/SeedDataOutput.tsx)。

主要流程：

- 初始化 `GameInfoProvider`
- 从 Dexie 读取配置
- 调用 `provideAll()` 生成整包种子信息
- 通过 `GameInfoContext` 向各详情视图分发

显示层在 [src/components/SeedInfo/SeedInfo.tsx](/f:/noita-tools/src/components/SeedInfo/SeedInfo.tsx) 及 `SeedInfoViews` 子组件中。

### 5.2 搜索页

入口在 [src/components/SearchSeeds/SearchSeeds.tsx](/f:/noita-tools/src/components/SearchSeeds/SearchSeeds.tsx)。

核心组成：

- `RuleList`：规则树展示与切换
- `RuleConstructor`：规则编辑器
- `SearchContextProvider`：搜索状态与执行调度中心

规则树相关文件：

- [src/components/SearchSeeds/ruleReducer.tsx](/f:/noita-tools/src/components/SearchSeeds/ruleReducer.tsx)
- [src/components/SearchSeeds/ruleNormalization.ts](/f:/noita-tools/src/components/SearchSeeds/ruleNormalization.ts)
- [src/components/SearchSeeds/node.ts](/f:/noita-tools/src/components/SearchSeeds/node.ts)

### 5.3 计算池页

入口在 [src/components/Compute](/f:/noita-tools/src/components/Compute)。

这部分主要提供：

- 本机作为 Host / Worker 的状态展示
- 连接主站后的任务接收与执行
- 简单的运行信息显示

## 6. 搜索运行链路

### 6.1 搜索状态中心

[src/components/SearchSeeds/SearchContext.tsx](/f:/noita-tools/src/components/SearchSeeds/SearchContext.tsx) 是搜索页核心调度器，负责：

- 规则树状态
- 搜索范围、最大结果数、模式配置
- `ChunkProvider`
- 浏览器本地 `SeedSolver`
- 本地多线程开关
- 计算池连接
- 计算任务隔离键 `computeJobHash`

当前 `computeJobHash` 由这些内容参与计算：

- `from`
- `to`
- `maxResults`
- `isNightmare`
- `rules`
- `customSeedList`

只要这些值变化，就会被视为新的搜索任务。

### 6.2 本地搜索

本地搜索走 [src/services/compute/CallbackComputeHandler.ts](/f:/noita-tools/src/services/compute/CallbackComputeHandler.ts)。

流程：

1. `ChunkProvider` 产生任务块
2. `SeedSolver.searchChunk()` 将任务分发给多个 Worker
3. Worker 内运行 `SeedSearcher.findSync()`
4. 结果回写到 `ChunkProvider.commitChunk()`

### 6.3 集群协同搜索

浏览器作为 Host 时，还会挂载 [src/services/compute/SocketComputeProvider.ts](/f:/noita-tools/src/services/compute/SocketComputeProvider.ts)。

它负责：

- 对外提供任务块，响应 `compute:get_job`
- 接收远端 Worker 回传结果，处理 `compute:done`

关键原则：

- 任务块仍由本地 `ChunkProvider` 分发
- 远端 Worker 只是额外算力来源
- 本地计算与远端计算可以同时进行

### 6.4 任务隔离

当前有两层保护：

- `SearchContext.tsx` 生成基于搜索配置的 `computeJobHash`
- `SocketComputeProvider` 在回收结果时校验 `jobName`

这能避免旧任务结果混入当前搜索。

## 7. Worker 与并行执行

### 7.1 浏览器 Worker

[src/services/seedSolverHandler.ts](/f:/noita-tools/src/services/seedSolverHandler.ts) 创建浏览器 Worker。

- 入口文件：[src/workers/seedSearcher.worker.ts](/f:/noita-tools/src/workers/seedSearcher.worker.ts)
- 通讯方式：`comlink`

### 7.2 Node Worker

[src/services/seedSolverHandler.node.ts](/f:/noita-tools/src/services/seedSolverHandler.node.ts) 创建 Node Worker。

- 入口文件：[src/workers/seedSearcher.worker.node.ts](/f:/noita-tools/src/workers/seedSearcher.worker.node.ts)
- 通讯方式：`worker_threads` + `comlink/dist/umd/node-adapter.js`

### 7.3 分块策略

`SeedSolver.searchChunk()` 的分块逻辑大致是：

- 普通规则时按 Worker 数量分块
- 含地图规则时缩小单块规模，降低图像生成和 GC 压力

地图相关规则的性能成本明显更高。

## 8. `SeedSearcher` 与规则执行

[src/services/seedSearcher.ts](/f:/noita-tools/src/services/seedSearcher.ts) 负责判断一个 seed 是否满足规则。

核心职责：

- 按成本排序规则，尽量先跑便宜规则
- 为当前 seed 设置世界随机种子
- 递归执行 `and/or/not`
- 调用 `gameInfoProvider.providers[rule.type].test(rule)`

这意味着新增规则时，最终必须落实到某个 Provider 的 `test()` 能力。

## 9. `GameInfoProvider` 与 Provider 体系

### 9.1 总入口

[src/services/SeedInfo/infoHandler/index.ts](/f:/noita-tools/src/services/SeedInfo/infoHandler/index.ts) 中的 `GameInfoProvider` 是种子信息系统总入口。

主要负责：

- 加载随机数模块 `loadRandom()`
- 动态导入各个 `InfoProvider`
- 按依赖顺序构建 Provider
- 在 `provideAll()` 中生成页面所需的整包数据
- 将普通/噩梦配置同步到各 Provider

### 9.2 主要 Provider

目录在 [src/services/SeedInfo/infoHandler/InfoProviders](/f:/noita-tools/src/services/SeedInfo/infoHandler/InfoProviders)。

关键 Provider 包括：

- `Alchemy`
- `AlwaysCast`
- `Biome`
- `ChestRandom`
- `EntranceWand`
- `Lottery`
- `Map`
- `PacifistChest`
- `Perk`
- `Potion`
- `Shop`
- `Spell`
- `Wand`
- `Weather`

### 9.3 依赖关系

`GameInfoProvider.buildInfoProviders()` 通过 `getDependencies()` 决定构建顺序。

已知关键依赖：

- `shop` 依赖 `wand` 与 `spells`
- `pacifistChest` 依赖 `chestRandom`
- `chestRandom` 依赖 `spells`
- `entranceWand` 依赖 `wand`
- `map` 额外依赖 `isNightmare`

新增 Provider 若有依赖，不要只改构造函数，也要同步改依赖声明。

## 10. 随机数、WASM 与图像处理

### 10.1 随机数入口

入口在 [src/services/SeedInfo/random/index.ts](/f:/noita-tools/src/services/SeedInfo/random/index.ts)。

当前会加载两套 WASM：

- `src/services/SeedInfo/noita_random/noita_random.wasm`
- `src/services/SeedInfo/wasm/rng.wasm`

之后由 [src/services/SeedInfo/random/random.ts](/f:/noita-tools/src/services/SeedInfo/random/random.ts) 做统一封装。

### 10.2 `random.ts` 的职责

它不只是透传 WASM，还实现了大量游戏级逻辑：

- 法术概率表读取
- 已解锁法术过滤
- `GetRandomAction`
- `GetRandomActionWithType`
- 加权随机选择
- 地图相关辅助对象

随机数或法术池行为偏差，优先检查这里。

### 10.3 图像处理

[src/services/imageActions/index.ts](/f:/noita-tools/src/services/imageActions/index.ts) 负责按运行环境切换图像实现：

- 浏览器：`webImageActions` / `webCanvasImageActions`
- Node：`nodeImageActions`

地图 Provider 高度依赖这层抽象。

### 10.4 高风险点

地图与 Node 图像解码是高风险区域，历史上已经出现过：

- `console-build` 中 wasm 路径缺失
- Node 图像导入路径错误
- 噩梦世界图 data URI 损坏导致 Node 解码崩溃

地图相关改动后，不要只测浏览器页面，也要验证 `console-build`。

## 11. 噩梦模式相关结构

噩梦模式不是普通模式的简单开关，而是明确依赖 `mods/nightmare` 的另一套生成体系。

已确认会受影响的内容：

- 世界地图
- 圣山层数与坐标
- 入口大厅
- 入口法杖
- 商店
- 和平主义宝箱
- 部分 perk、药水、敌人生成逻辑

代码分布重点：

- `GameInfoProvider.applyConfigToProviders()`
- 各 Provider 的 `setNightmareMode()`
- `holyMountainLocations.ts`
- `Map` Provider
- `Perk` / `Shop` / `PacifistChest` / `Lottery` / `AlwaysCast`

维护原则：

- 普通模式与噩梦模式不要共用错误的位置推导
- 显示层与搜索匹配层必须使用同一套坐标与层数逻辑
- 浏览器模式与 `console-build` 结果必须一致

## 12. 计算池服务端结构

[server/io/compute.mjs](/f:/noita-tools/server/io/compute.mjs) 是集群协议核心。

它维护：

- `counts.hosts`
- `counts.workers`
- `counts.appetite`
- socket 注册表
- `pendingJobs`

主要事件：

- `compute:host:register`
- `compute:worker:register`
- `compute:need_job`
- `compute:get_job`
- `compute:done`

### 12.1 版本兼容策略

当前采用“完整版本号必须一致”的策略，不再只比较主版本号。

目的：

- 阻止旧 Worker 混入新逻辑
- 阻止同主版本但内部规则已变化的节点继续参与搜索

### 12.2 结果隔离

回传结果带 `jobName`，Host 收到非当前任务的结果时会直接忽略。

这是协议层的重要安全垫。

## 13. 本地存储与数据库

[src/services/db.ts](/f:/noita-tools/src/services/db.ts) 使用 Dexie 管理浏览器 IndexedDB。

主要表：

- `configItems`
- `seedInfo`
- `favorites`
- `searches`

主要用途：

- 保存设置
- 保存单种子配置
- 保存收藏
- 保存搜索任务定义

当前数据库版本已到 `11`。新增迁移时只追加新版本，不要回写旧迁移逻辑。

## 14. 构建与打包

### 14.1 浏览器构建

[vite.config.ts](/f:/noita-tools/vite.config.ts) 关键点：

- `base: "./"` 支持相对路径部署
- `APP_VERSION` 来自 `package.json`
- `/socket.io` 与 `/api` 代理到 `localhost:3001`
- `vite-plugin-comlink` 用于 Worker 通讯
- `build.outDir = "./build"`

### 14.2 `console-build`

[consoleBuild.cjs](/f:/noita-tools/consoleBuild.cjs) 不只是编译脚本，还承担运行时修补职责：

- 修正 Worker 入口扩展名
- 修正 Node 图像导入路径
- 复制 `noita_random.wasm`
- 复制 `rng.wasm`
- 复制 `Alchemy.wasm`
- 复制 `FungalShift.wasm`
- 生成 `console-build/package.json`

因此它属于关键构建链路，不是普通打包辅助脚本。

### 14.3 Docker 主站

[Dockerfile](/f:/noita-tools/Dockerfile) 采用两阶段构建：

- builder：安装完整依赖并执行 `npm run build`
- production：保留生产依赖、`build/` 与 `server/`

### 14.4 Docker Worker

[Dockerfile.worker](/f:/noita-tools/Dockerfile.worker) 同样是两阶段：

- builder：执行 `npm run console-build`
- production：复制 `console-build/` 后运行 `consoleSearch.js`

### 14.5 GitHub Actions

[.github/workflows/build.yml](/f:/noita-tools/.github/workflows/build.yml) 当前流程：

- `npm ci --legacy-peer-deps`
- `npx tsc --noEmit`
- `npm run build`
- `npm run console-build`
- `bash ./scripts/pack-node.sh`
- `bash ./scripts/pack-nginx.sh`
- 上传 `console-build/`

CI 里显式使用 `bash`，是为了降低脚本执行权限问题对构建的影响。

## 15. `scripts` 目录职责

[scripts](/f:/noita-tools/scripts) 负责离线部署与打包。

主要脚本：

- `pack-node.*`
- `pack-nginx.*`
- `pack-docker.sh`
- `pack-worker.sh`
- `deploy-node.*`
- `deploy-nginx.*`
- `deploy-docker.*`
- `install-node.sh`

近期需要注意的事实：

- Node 部署包必须包含 `server/logger.mjs`
- `deploy-node` 已支持透传 `LOG_LEVEL` 与 `LOG_TIMEZONE`
- `pack-worker.sh` 只构建本地镜像，不导出压缩包

## 16. 测试结构

当前回归价值最高的测试主要在：

- `src/services/SeedInfo/infoHandler/InfoProviders/*.spec.ts`
- `src/services/seedSearcher.spec.ts`

适合优先补测试的改动类型：

- 生成逻辑
- 搜索匹配逻辑
- 噩梦模式
- 圣山布局
- 集群协议

## 17. 常见维护场景

### 17.1 升级版本号

至少检查：

- [package.json](/f:/noita-tools/package.json)
- [Dockerfile](/f:/noita-tools/Dockerfile)
- `vite.config.ts` 的 `APP_VERSION`
- `consoleBuild.cjs` 对 `console-build/package.json` 的生成
- `server/io/compute.mjs` 的版本校验

### 17.2 升级 Node 版本

至少检查：

- `package.json.engines.node`
- `Dockerfile`
- `Dockerfile.worker`
- `consoleBuild.cjs` 目标配置
- GitHub Actions `setup-node`

### 17.3 新增一个 Provider

至少涉及：

1. `InfoProviders/` 下新增文件
2. `GameInfoProvider` 类型定义
3. `buildInfoProviders()` 的导入映射
4. 如有依赖，更新 `getDependencies()`
5. 如需页面展示，更新 `provideAll()` 与详情视图
6. 如需搜索支持，实现 `test(rule)`

### 17.4 新增一个搜索规则

通常需要改：

1. `SearchViews/` 下的规则 UI
2. `ruleReducer.tsx` / `ruleNormalization.ts`
3. 某个 Provider 的 `test(rule)`
4. `seedSearcher.ts` 中的 `searchWeights`

### 17.5 修改噩梦模式逻辑

优先检查：

- `mods/nightmare` 真实脚本来源
- `holyMountainLocations.ts`
- `Shop.ts`
- `PacifistChest.ts`
- `Lottery.ts`
- `Perk.ts`
- `Map` Provider
- `GameInfoProvider.provideAll()`
- 详情页与搜索页是否共用同一套层数/坐标逻辑

### 17.6 修改圣山层数或坐标

必须同时修改显示层与计算层：

- `holyMountainLocations.ts`
- 圣山详情展示组件
- 商店 Provider
- 天赋 Provider
- 和平主义宝箱 Provider
- 搜索匹配逻辑

原则：

- 不要在多个 Provider 内各自硬编码坐标
- 尽量收敛到共享位置函数

### 17.7 修改集群协议

至少检查：

- `server/io/compute.mjs`
- `src/services/compute/ComputeSocket.ts`
- `src/services/compute/SocketComputeProvider.ts`
- `src/components/SearchSeeds/SearchContext.tsx`
- `src/consoleSearch.ts`

重点确认：

- Host 发出的 job 结构
- Worker 回传结构
- `jobName` 是否保留
- 版本不一致是否继续拒绝

### 17.8 修改 `console-build`

优先检查：

- `consoleBuild.cjs`
- `search.package.json`
- `src/consoleSearch.ts`
- `src/services/seedSolverHandler.node.ts`
- `src/workers/seedSearcher.worker.node.ts`

建议的最小验证：

1. `npm run console-build`
2. 直接运行 `node console-build/consoleSearch.js --help`

## 18. 建议维护顺序

做较大改动时，建议按这个顺序推进：

1. 先判断改动属于 UI、Provider、随机数、地图、搜索、协议还是构建层
2. 优先找到共享入口，不要在多个地方复制修补
3. 先让浏览器本地搜索正确，再验证集群与 `console-build`
4. 涉及噩梦模式时，同时回归普通模式
5. 涉及圣山、商店、天赋、宝箱时，同时验证详情页和搜索匹配逻辑
6. 涉及 Node/Worker 路径时，必须额外验证 `console-build`

## 19. 建议阅读顺序

如果要快速建立整体理解，建议按这个顺序读代码：

1. [package.json](/f:/noita-tools/package.json)
2. [src/components/App.tsx](/f:/noita-tools/src/components/App.tsx)
3. [src/components/Body.tsx](/f:/noita-tools/src/components/Body.tsx)
4. [src/components/SeedInfo/SeedDataOutput.tsx](/f:/noita-tools/src/components/SeedInfo/SeedDataOutput.tsx)
5. [src/components/SearchSeeds/SearchContext.tsx](/f:/noita-tools/src/components/SearchSeeds/SearchContext.tsx)
6. [src/services/seedSearcher.ts](/f:/noita-tools/src/services/seedSearcher.ts)
7. [src/services/SeedInfo/infoHandler/index.ts](/f:/noita-tools/src/services/SeedInfo/infoHandler/index.ts)
8. [src/services/SeedInfo/random/random.ts](/f:/noita-tools/src/services/SeedInfo/random/random.ts)
9. [server/io/compute.mjs](/f:/noita-tools/server/io/compute.mjs)
10. [consoleBuild.cjs](/f:/noita-tools/consoleBuild.cjs)

## 20. 总结

这个项目的复杂度主要不在页面，而在这些交叉地带：

- 普通模式与噩梦模式逻辑分叉
- 详情页展示逻辑与搜索匹配逻辑必须一致
- 浏览器路径与 `console-build` 路径必须一致
- 计算池协议升级后旧 Worker 不能混入
- 原始数据更新后内部快照与 Provider 也要同步

理解入口文件、Provider 总线、搜索执行器、随机数/WASM、集群协议和 `console-build` 这几块，后续维护成本会明显下降。
