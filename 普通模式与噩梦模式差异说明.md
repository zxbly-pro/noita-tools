# 普通模式与噩梦模式差异说明

本文档整理本项目中“普通模式”和“噩梦模式（`mods/nightmare`）”的主要差异，重点覆盖：

- 世界地图与圣山结构
- 入口大厅与入口法杖
- 法杖与法术生成逻辑
- Perk、药水、敌人与玩家数值差异
- 这些差异对项目实现的影响

本文只记录当前仓库内可以直接验证的内容，以及本次排查后已确认的实现结论。

## 1. 模式切换的根本区别

### 1.1 普通模式

- 主要依赖根目录 `data/`
- 使用原版世界地图与原版圣山 / 入口大厅逻辑
- 不会在入口大厅生成噩梦专属的 3 根入口法杖

### 1.2 噩梦模式

- 核心差异来自 `mods/nightmare/`
- 不是“普通模式调高倍率”这么简单，而是：
  - 改世界地图
  - 改部分 biome 分布
  - 改圣山逻辑
  - 改入口法杖
  - 改 perk 列表
  - 改药水逻辑
  - 改敌人附加 perk / 法杖逻辑
  - 改玩家承伤

## 2. 数据与脚本来源

### 2.1 主要目录

- 普通模式基础数据：`data/`
- 噩梦模式附加内容：`mods/nightmare/`
- 项目内普通 / 噩梦地图切换实现：
  - `src/services/SeedInfo/infoHandler/InfoProviders/Map/index.ts`
  - `src/services/SeedInfo/infoHandler/InfoProviders/Map/nightmareWorldMap.ts`

### 2.2 关键噩梦脚本

- `mods/nightmare/init.lua`
- `mods/nightmare/files/magic_numbers.xml`
- `mods/nightmare/files/biome_map.lua`
- `mods/nightmare/data/scripts/biomes/mountain/mountain_hall.lua`
- `mods/nightmare/files/perk_list_appends.lua`
- `mods/nightmare/files/potion_appends.lua`
- `mods/nightmare/files/director_helpers_appends.lua`

## 3. 世界地图差异

### 3.1 地图入口点不同

噩梦模式通过 `mods/nightmare/files/magic_numbers.xml` 覆盖：

```text
BIOME_MAP="mods/nightmare/files/biome_map.lua"
```

这说明噩梦模式直接替换了世界生物群系地图，不是普通模式上的增量修补。

### 3.2 噩梦地图脚本特点

`mods/nightmare/files/biome_map.lua` 中可以直接看到：

- 地图大小设为 `64 x 48`
- 先加载 `mods/nightmare/files/biome_map.png`
- 再对多个区域做重绘、交换和支线洞窟追加

脚本中显式存在这些变化：

- `biome_coalmines` 与 `biome_excavationsite` 可互换
- `biome_fungicave` 与 `biome_excavationsite` 可互换
- `biome_snowcaves` 与 `biome_hiisibase` 可互换
- `biome_jungle_1` 与 `biome_jungle_2` 可互换
- `biome_sandcaves` 与 `biome_fungicave` 可互换
- `biomes_wandcave` 与 `biome_sandcaves` 可互换
- 左右两侧会重绘 Fungal Cave / Wand Cave / Sand Cave 类支线洞窟
- Orb、Boss Arena、End Room 的像素位置会重设

因此噩梦模式地图结构不是与普通模式一一对应的静态图。

### 3.3 圣山与主路径差异

结合当前项目实现和实际游戏验证，可得出：

- 普通模式按 7 层主圣山处理
- 噩梦模式按 5 层主圣山处理
- 噩梦模式最下方仍有一层特殊传送门通向 boss 房前的附加圣山
- 噩梦模式每层区域通常更大

对工具的直接要求是：

- 普通模式与噩梦模式不能共用同一套圣山层级推导
- 商店、天赋、和平主义宝箱都必须按各自模式的圣山布局处理
- 详情页层数展示与搜索匹配必须完全共用同一份布局逻辑

### 3.4 项目中的地图切换实现

当前项目地图 Provider 已支持：

- 普通模式读取普通世界图
- 噩梦模式读取 `nightmareWorldMap.ts`

对应代码：

- `src/services/SeedInfo/infoHandler/InfoProviders/Map/index.ts`

`GameInfoProvider.updateConfig()` 在切换 `isNightmare` 时也会同步更新地图 Provider。

## 4. 入口大厅差异

### 4.1 普通模式入口大厅

普通模式入口大厅脚本：

- `data/scripts/biomes/mountain/mountain_hall.lua`

包含普通入口场景、`spawn_all_perks` 等逻辑，但不会生成噩梦入口法杖。

### 4.2 噩梦模式入口大厅

噩梦入口大厅脚本：

- `mods/nightmare/data/scripts/biomes/mountain/mountain_hall.lua`

其中多了：

- `RegisterSpawnFunction(0xff33934c, "spawn_all_shopitems")`

并定义：

- `spawn_all_shopitems(x, y)`

该函数会在入口大厅生成 3 根专属法杖。

### 4.3 入口法杖模板集合

`spawn_all_shopitems` 中的候选模板：

- `wand_level_02`
- `wand_level_02_better`
- `wand_level_03`
- `wand_unshuffle_01`
- `wand_unshuffle_02`
- `wand_unshuffle_03`

生成间距：

- `width = 132`
- `count = 3`
- `item_width = 44`

因此横向坐标是：

- `x`
- `x + 44`
- `x + 88`

### 4.4 拾取行为

`mods/nightmare/files/nightmare_starting_wand.lua` 定义了特殊行为：

- 玩家拾取任意一根带 `nightmare_starting_wand` 标签的法杖后
- 剩余仍在场景中的噩梦起始法杖会被 `EntityKill`

本质上是“三选一”。

## 5. 入口法杖坐标差异

### 5.1 不能沿用普通模式入口点

噩梦入口法杖不能使用普通模式入口大厅的旧坐标推导。

关键依据：

- 标记色位于 `mods/nightmare/data/biome_impl/mountain/hall.png`
- 对应颜色：`0xff33934c`
- 标记像素位置：`(191, 418)`

### 5.2 当前确认的基准坐标

结合当前项目实现与实际种子验证，噩梦入口法杖基准坐标修正为：

- `SPAWN_X = 703`
- `SPAWN_Y = -94`

其余两根按 `44` 的横向间距排列。

当前实现对应：

- `src/services/SeedInfo/infoHandler/InfoProviders/EntranceWand.ts`
- `src/services/SeedInfo/infoHandler/index.ts`

## 6. 入口法杖生成逻辑差异

### 6.1 这 3 根法杖只存在于噩梦模式

这一点非常重要：

- 普通模式没有这 3 根法杖
- 这 3 根法杖来源于 `mods/nightmare`
- 不能套用普通圣山商店法杖或普通入口大厅法杖逻辑

### 6.2 `Random(1, opts)` 的兼容处理

噩梦入口大厅脚本里实际写的是：

```lua
local opt = Random(1, opts)
```

不是：

```lua
local opt = Random(1, #opts)
```

在当前项目实现中，这被单独兼容为稳定命中第一个模板的行为，用来贴合实机结果。

### 6.3 `wand_level_02_better` 需要独立流程

这些模板里：

- `wand_level_02` 使用 `gun_procedural.lua`
- `wand_unshuffle_02` 也用 `gun_procedural.lua`，但 `force_unshuffle = true`
- `wand_level_02_better` 使用 `gun_procedural_better.lua`

这不是简单数值微调，而是另一套程序法杖生成流程。

当前项目已将其拆为独立 `procedure`：

- `default`
- `better`

对应代码：

- `src/services/SeedInfo/infoHandler/InfoProviders/Wand.ts`

## 7. 通用法杖生成链路

以下逻辑同时影响普通法杖与噩梦入口法杖。

### 7.1 默认流程

普通程序法杖脚本：

- `data/scripts/gun/procedural/gun_procedural.lua`

例如：

- `wand_level_02.lua` => `generate_gun(40, 2, false)`
- `wand_unshuffle_02.lua` => `generate_gun(40, 2, true)`

主流程大致是：

1. 读取实体坐标 `(x, y)`
2. `SetRandomSeed(x, y)`
3. 调 `get_gun_data(cost, level, force_unshuffle)`
4. `make_wand_from_gun_data(...)`
5. `wand_add_random_cards(...)`

### 7.2 `better` 流程的区别

`gun_procedural_better.lua` 相对普通版有这些差别：

- 属性概率表更紧
- 容量更偏向高区间
- 充能与延迟分布不同
- 卡牌填充顺序更结构化

因此不能把 `better` 视为普通流程的轻微变体。

## 8. 法术抽取逻辑差异

### 8.1 原始问题来源

此前工具在 `GetRandomActionWithType` 上直接按 `spells.json` 顺序做线性减权重，和游戏实际行为不一致。

后果是：

- 法杖基础属性可能已经很接近
- 但内部法术完全错误

### 8.2 正确实现更接近 `spells.h`

当前修正思路是：

- 每个 level / type 使用预排序的累计概率表
- 通过 `seededRandom` 得到 cutoff
- 在累计表中定位命中项

而不是直接遍历 `spells.json` 原始顺序。

### 8.3 对噩梦入口法杖的影响

这对入口法杖影响极大。

以种子 `10003796` 的第一根入口法杖为例：

- 坐标修正到 `(703, -94)` 后
- 如果仍按旧 `spells.json` 顺序抽取，内部法术仍会错误
- 改为参考累计概率表后，才能稳定命中 `SUMMON_HOLLOW_EGG`

## 9. 已确认的实机对齐样例

以用户提供的种子 `10003796` 为例，第一根噩梦入口法杖已确认正确结果应为：

- `Shuffle = 是`
- `Spells/Cast = 2`
- `Cast delay = 0.20 s`
- `Rechrg. Time = 2.17 s`
- `Mana max = 310`
- `Mana chg. Spd = 109`
- `Capacity = 12`
- `Spread = -4.0°`
- 内部法术：`SUMMON_HOLLOW_EGG x8`

要得到这一结果，至少要同时满足：

- 噩梦坐标使用 `(703, -94)`
- 模板使用 `wand_level_02`
- RNG 行为与参考实现一致
- 法术抽取使用累计概率表

## 10. Perk 差异

### 10.1 噩梦模式移除 `INVISIBILITY`

文件：

- `mods/nightmare/files/perk_list_appends.lua`

会移除：

- `INVISIBILITY`

因此工具在噩梦模式下也必须显式忽略它。

当前项目对应：

- `GameInfoProvider.updateConfig()`
- `nightmarePerks = ["INVISIBILITY"]`

### 10.2 噩梦入口 perk

项目中噩梦模式除了入口法杖，还存在入口 perk 生成流程：

- `generateEntrancePerks(...)`
- `initialPerkIndex = 3`

这和普通模式直接进入常规圣山 perk 流程不同。

## 11. 玩家承伤差异

`mods/nightmare/init.lua` 的 `OnPlayerSpawned()` 会提高玩家对多类伤害的承伤倍率。

已知包括：

- `melee * 3`
- `projectile * 2`
- `explosion * 2`
- `electricity * 2`
- `fire * 2`
- `drill * 2`
- `slice * 2`
- `ice * 2`
- `radioactive * 2`
- `poison * 3`

这意味着噩梦模式对玩家的容错显著更低。

## 12. 敌人与 biome 数值差异

`mods/nightmare/init.lua` 的 `OnWorldInitialized()` 会对多个 biome 调用：

- `BiomeSetValue(..., "game_enemy_hp_scale", hp_scale)`
- `BiomeSetValue(..., "game_enemy_attack_speed", attack_speed)`

涉及的 biome 包括：

- `clouds`
- `coalmine`
- `coalmine_alt`
- `crypt`
- `desert`
- `excavationsite`
- `forest`
- `fungicave`
- `pyramid`
- `rainforest`
- `rainforest_open`
- `sandcave`
- `snowcastle`
- `snowcave`
- `snowcave_tunnel`
- `the_end`
- `the_sky`
- `vault`
- `vault_frozen`
- `wandcave`

示例：

- `coalmine`：`hp_scale = 3`
- `vault`：`hp_scale = 12.5`
- `wandcave`：`hp_scale = 16`
- `the_end` / `the_sky`：`hp_scale = 50`

## 13. 药水逻辑差异

`mods/nightmare/files/potion_appends.lua` 重写了药水初始化。

主流程：

1. `SetRandomSeed(x, y)`
2. 50% 从 `materials_magic` 抽
3. 否则从 `materials_standard` 抽
4. 再检查 `VariableStorageComponent` 中是否有强制指定的 `potion_material`
5. 调 `AddMaterialInventoryMaterial(...)`

噩梦模式明显提高了魔法药液出现权重，不能套普通模式预期。

## 14. 敌人附加 perk / 法杖差异

`mods/nightmare/files/director_helpers_appends.lua` 会改写敌人生成辅助逻辑。

### 14.1 敌人获得 perk

在 `entity_load_camera_bound(...)` 中：

- 有 `1 / 15` 概率启用 `giveperk = true`
- 从 `perk_list` 中筛选 `usable_by_enemies = true` 的 perk
- 随机挂到敌人身上

### 14.2 敌人获得法杖

同一逻辑里：

- 有 `1 / 20` 概率启用 `givewand = true`
- 按敌人所在纵深估算 wand level
- 50% 在 `wand_level_xx` 与 `wand_unshuffle_xx` 间二选一

噩梦模式下同一敌人的威胁水平可能远高于普通模式。

## 15. Magic Numbers 差异

`mods/nightmare/files/magic_numbers.xml` 除了替换 `BIOME_MAP`，还修改了：

- `DAMAGE_BLOOD_AMOUNT_MIN="5"`
- `DAMAGE_BLOOD_AMOUNT_MAX="10"`
- `RAGDOLL_BLOOD_MULTIPLIER="1"`

这是系统级全局覆盖。

## 16. 对项目实现的直接要求

在本项目里，普通模式与噩梦模式至少必须分开处理这些模块：

- 世界地图
- 圣山层级与坐标
- 入口大厅
- 入口 perk
- 入口法杖
- 噩梦专属 perk 忽略列表
- 商店
- 和平主义宝箱

可以共用但必须保证算法正确的部分：

- RNG 核心
- 通用程序法杖生成
- 法术累计概率表抽取

## 17. 当前已落地的关键修复

截至当前仓库状态，已完成的关键修复包括：

- `MapInfoProvider` 已支持 `isNightmare`
- 噩梦入口法杖坐标修正到 `(703, -94)`
- `wand_level_02_better` 已独立为 `better` 过程
- RNG 行为已朝参考实现校正
- 法术抽取已改用累计概率表
- 圣山层数展示已区分普通 / 噩梦
- 商店、和平主义宝箱从第二层开始的噩梦偏差已按圣山布局修正
- 噩梦最下方额外圣山已补回
- 平行世界相关按钮、逻辑、页面已移除，不再参与详情页与搜索

## 18. 后续维护建议

### 18.1 不要把噩梦模式当成普通模式上的布尔修饰

建议至少拆开这些层：

- 地图层
- 圣山布局层
- 入口层
- 商店 / 宝箱层
- 法杖入口层

### 18.2 入口法杖优先以实机结果为准

对噩梦入口法杖，必须同时满足：

- 坐标准确
- RNG 准确
- 法术表顺序准确
- 模板选择行为准确

任何一项错误都会导致整根法杖偏差。

### 18.3 所有来自 `mods/nightmare` 的生成内容，都不要复用普通模式经验值

例如：

- 圣山结构
- 入口法杖
- 药水
- 敌人附加 perk / 法杖

都应优先回到 `mods/nightmare` 原脚本核对。

## 19. 模块分叉矩阵

| 模块 | 普通模式 | 噩梦模式 | 是否必须分叉 | 备注 |
| --- | --- | --- | --- | --- |
| 世界地图源 | 原版 world map | `nightmareWorldMap.ts` / `mods/nightmare/files/biome_map.lua` | 是 | 不能共用 |
| 圣山层级理解 | 7 层主圣山 | 5 层主圣山 + 最下方附加圣山 | 是 | 布局不同 |
| 入口大厅脚本 | `data/scripts/biomes/mountain/mountain_hall.lua` | `mods/nightmare/.../mountain_hall.lua` | 是 | 噩梦多了入口法杖 |
| 入口 perk | 普通流程 | 有 `generateEntrancePerks` + `initialPerkIndex=3` | 是 | 已分叉 |
| 入口法杖 | 不存在 | 存在 3 根专属法杖 | 是 | 已分叉 |
| 程序法杖生成 | `gun_procedural.lua` | 仍会复用 | 否 | 但噩梦入口依赖准确接线 |
| `wand_level_02_better` | 可选 | 入口逻辑直接依赖 | 是 | 必须独立过程 |
| RNG 核心 | 统一 | 统一 | 否 | 但必须与游戏一致 |
| 法术抽取 | 统一 | 统一 | 否 | 必须用累计概率表 |
| Perk 忽略列表 | 默认 | 忽略 `INVISIBILITY` | 是 | 已分叉 |
| 药水生成 | 原版 | `potion_appends.lua` | 是 | 权重不同 |
| 敌人附加 perk / 法杖 | 原版 | `director_helpers_appends.lua` | 是 | 威胁更高 |

## 20. 入口法杖排查清单

如果以后某个种子的噩梦入口法杖再次对不上，建议按这个顺序排查：

### 20.1 先查位置

- 当前入口大厅是否确实是噩梦地图
- `mountain_hall` 的 tile 是否仍是当前实现所用位置
- `hall.png` 中 `spawn_all_shopitems` 标记是否变化
- 基准坐标是否仍应为 `(703, -94)`
- 后两根是否仍按 `44` 间距排列

### 20.2 再查模板

- 脚本是否仍写成 `Random(1, opts)`
- 工具是否仍按固定第一模板兼容
- 如果未来确认实机不是固定第一模板，这里需要重新实现

### 20.3 再查 RNG

- `SetWorldSeed`
- `SetRandomSeed`
- `Random`
- `RandomDistribution`
- `RandomDistributionf`
- `SeededRandom`

只要这里偏了，基础属性通常会整体漂移。

### 20.4 再查法术表

- 是否仍使用 `spellProbTables.json`
- 是否仍对应参考 `spells.h`
- `GetRandomActionWithType` 是否仍按累计概率表抽取
- `unlockedSpells` 是否正确初始化

### 20.5 最后查 `better` 流程

- 当前模板到底是 `wand_level_02` 还是 `wand_level_02_better`
- `procedure` 是否传对
- `better` 流程中的卡牌填充顺序是否被破坏

## 21. 结论

以种子 `10003796` 为代表，本次排查已经足够说明：

1. 噩梦入口法杖是 `mods/nightmare` 专属内容，不属于普通模式逻辑。
2. 圣山层级、商店、和平主义宝箱、入口大厅都必须按噩梦世界布局单独推导。
3. 入口法杖正确结果依赖坐标、RNG、模板选择和法术抽取四层同时正确。
4. 普通模式与噩梦模式在项目内部必须作为两套相关但不可随意混用的生成体系维护。
