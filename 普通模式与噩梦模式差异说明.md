# 普通模式与噩梦模式差异说明

本文档整理本项目中“普通模式”和“噩梦模式（`mods/nightmare`）”的主要差异，重点覆盖：

- 地图与圣山结构差异
- 入口大厅与入口法杖差异
- 法杖生成逻辑差异
- Perk、药水、敌人、玩家承伤差异
- 这些差异对种子工具实现的影响

本文只记录当前仓库内可以直接验证的内容，以及本次排查中已经确认的实现结论。

## 1. 模式切换的根本区别

### 1.1 普通模式

- 主要依赖根目录 `data/`
- 使用原版世界地图与原版圣山/入口大厅逻辑
- 不会在入口大厅生成噩梦专属 3 根入口法杖

### 1.2 噩梦模式

- 核心差异来自 `mods/nightmare/`
- 不是“普通模式加几个倍率”，而是：
  - 改世界地图
  - 改若干 biome 数值
  - 改玩家承伤
  - 改 perk 列表
  - 改药水逻辑
  - 改敌人附加 perk / 法杖逻辑
  - 在入口大厅追加 3 根专属法杖

## 2. 数据与脚本来源

### 2.1 主要目录

- 普通模式基础数据：`data/`
- 噩梦模式附加内容：`mods/nightmare/`
- 项目内普通/噩梦地图切换实现：
  - `src/services/SeedInfo/infoHandler/InfoProviders/Map/index.ts`
  - `src/services/SeedInfo/infoHandler/InfoProviders/Map/nightmareWorldMap.ts`

### 2.2 噩梦模式入口脚本

关键脚本如下：

- `mods/nightmare/init.lua`
- `mods/nightmare/files/magic_numbers.xml`
- `mods/nightmare/files/biome_map.lua`
- `mods/nightmare/data/scripts/biomes/mountain/mountain_hall.lua`
- `mods/nightmare/files/perk_list_appends.lua`
- `mods/nightmare/files/potion_appends.lua`
- `mods/nightmare/files/director_helpers_appends.lua`

## 3. 世界地图差异

## 3.1 世界地图入口点不同

噩梦模式通过：

- `mods/nightmare/files/magic_numbers.xml`

把：

- `BIOME_MAP="mods/nightmare/files/biome_map.lua"`

写入游戏魔法数配置。

这意味着噩梦模式直接替换了世界生物群系地图，不是基于普通模式地图做增量修补。

## 3.2 噩梦地图脚本的结构特点

`mods/nightmare/files/biome_map.lua` 中可以直接看到：

- 地图大小设为 `64 x 48`
- 先加载 `mods/nightmare/files/biome_map.png`
- 再用脚本对多个区域进行重绘、交换和支线洞穴追加

脚本中显式存在这些变化：

- `biome_coalmines` 与 `biome_excavationsite` 直接互换
- `biome_fungicave` 与 `biome_excavationsite` 有概率互换
- `biome_snowcaves` 与 `biome_hiisibase` 有概率互换
- `biome_jungle_1` 与 `biome_jungle_2` 有概率互换
- `biome_sandcaves` 与 `biome_fungicave` 有概率互换
- `biomes_wandcave` 与 `biome_sandcaves` 有概率互换
- 左右两侧会重新绘制 Fungal Cave / Wand Cave / Sand Cave 类型支线洞穴
- Orb、Boss Arena、End Room 的像素位置会重新设置

因此，噩梦模式的地图结构不是静态地和普通模式一一对应，而是有额外的脚本重排过程。

## 3.3 圣山/主路径规模差异

从项目当前的噩梦地图支持与用户给出的实际情况来看：

- 普通模式主路径可按 7 层圣山理解
- 噩梦模式主路径按 5 层圣山理解
- 噩梦模式每一层通常更大

对工具来说，重要结论不是“层数具体怎么描述”，而是：

- 普通模式和噩梦模式绝不能共用同一套圣山层级与坐标推导
- 噩梦模式入口大厅、圣山、支线地图都必须按噩梦世界图单独处理

## 3.4 本项目中的地图切换实现

项目中地图 provider 已支持：

- 普通模式读取普通世界图
- 噩梦模式读取 `nightmareWorldMap.ts`

对应代码：

- `src/services/SeedInfo/infoHandler/InfoProviders/Map/index.ts`

`GameInfoProvider.updateConfig()` 中也会在切换 `isNightmare` 时同步切换地图 provider。

## 4. 入口大厅差异

## 4.1 普通模式入口大厅

普通模式入口大厅脚本：

- `data/scripts/biomes/mountain/mountain_hall.lua`

其中包含：

- 常规入口像素场景
- `spawn_all_perks`
- 教学交互相关触发器

普通模式不会在入口大厅生成噩梦专属入口法杖。

## 4.2 噩梦模式入口大厅

噩梦模式入口大厅脚本：

- `mods/nightmare/data/scripts/biomes/mountain/mountain_hall.lua`

与普通模式相比，多了：

- `RegisterSpawnFunction(0xff33934c, "spawn_all_shopitems")`

并定义了：

- `spawn_all_shopitems(x, y)`

该函数会在入口大厅生成 3 根专属法杖。

## 4.3 噩梦入口法杖模板集合

`spawn_all_shopitems` 中定义的候选模板为：

- `wand_level_02`
- `wand_level_02_better`
- `wand_level_03`
- `wand_unshuffle_01`
- `wand_unshuffle_02`
- `wand_unshuffle_03`

每根法杖横向间隔为：

- `width = 132`
- `count = 3`
- `item_width = 44`

因此 3 根法杖的横向生成点是：

- `x`
- `x + 44`
- `x + 88`

## 4.4 入口大厅法杖拾取行为

`mods/nightmare/files/nightmare_starting_wand.lua` 中定义了一个特殊行为：

- 玩家拾取其中一根带 `nightmare_starting_wand` 标签的法杖后
- 其余仍在世界中的噩梦起始法杖会被 `EntityKill`

也就是说，这 3 根法杖本质上是“起始三选一”。

## 5. 入口大厅坐标差异

## 5.1 噩梦入口法杖不能使用普通模式入口点

本次排查确认，噩梦入口法杖不能使用原来工具中的旧坐标推导。

关键依据：

- 标记色位于 `mods/nightmare/data/biome_impl/mountain/hall.png`
- 对应颜色：`0xff33934c`
- 标记局部像素位置：`(191, 418)`

## 5.2 当前确认的噩梦入口法杖基准坐标

结合项目当前噩梦地图区域定位，最终修正为：

- `SPAWN_X = 703`
- `SPAWN_Y = -94`

并按 `44` 的间距生成后两根。

项目中对应实现：

- `src/services/SeedInfo/infoHandler/InfoProviders/EntranceWand.ts`
- `src/services/SeedInfo/infoHandler/index.ts`

## 6. 入口法杖生成逻辑差异

## 6.1 这些法杖只在噩梦模式存在

这一点非常重要：

- 普通模式没有这 3 根法杖
- 噩梦模式的这 3 根法杖来源于 `mods/nightmare`
- 所以它们不应该套用普通模式“圣山商店法杖”或“普通入口大厅”的任何推导逻辑

## 6.2 候选模板脚本存在特殊写法

噩梦入口大厅脚本里实际写的是：

```lua
local opt = Random( 1, opts )
```

而不是：

```lua
local opt = Random( 1, #opts )
```

在本项目当前修复里，这被视为一个必须单独兼容的行为：

- 工具侧目前将其模拟为稳定选取第一个模板
- 即入口 3 根法杖都按 `wand_level_02` 逻辑生成

这样做的原因是：

- 它能与实际游戏中用户给出的第一根法杖结果对齐
- 如果按“正常随机 6 选 1”处理，第一根法杖模板和结果会对不上

## 6.3 `wand_level_02_better` 需要独立处理

这些模板中：

- `wand_level_02` 使用 `gun_procedural.lua`
- `wand_unshuffle_02` 也是普通 `gun_procedural.lua`，但 `force_unshuffle = true`
- `wand_level_02_better` 使用的是：
  - `data/scripts/gun/procedural/gun_procedural_better.lua`

这不是简单的数值微调，而是另一条程序法杖流程。

项目中已将它拆成独立 `procedure`：

- `default`
- `better`

对应代码：

- `src/services/SeedInfo/infoHandler/InfoProviders/Wand.ts`

## 7. 法杖生成链路说明

以下内容对普通模式法杖和噩梦入口法杖都适用，但噩梦入口法杖更依赖其正确实现。

## 7.1 默认法杖流程 `generate_gun(cost, level, force_unshuffle)`

普通程序法杖脚本：

- `data/scripts/gun/procedural/gun_procedural.lua`

入口模板：

- `wand_level_02.lua` => `generate_gun(40, 2, false)`
- `wand_unshuffle_02.lua` => `generate_gun(40, 2, true)`

主流程：

1. 读取实体坐标 `(x, y)`
2. `SetRandomSeed(x, y)`
3. 调用 `get_gun_data(cost, level, force_unshuffle)` 生成基础法杖属性
4. `make_wand_from_gun_data(...)` 写回 AbilityComponent
5. `wand_add_random_cards(...)` 填充内置法术

## 7.2 基础属性生成步骤

`get_gun_data()` 的重要逻辑：

1. `level == 1` 时有 50% 概率额外 `cost + 5`
2. 再执行 `cost += Random(-3, 3)`
3. 初始 mana：
   - `mana_charge_speed = 50 * level + Random(-5, 5 * level)`
   - `mana_max = 50 + 150 * level + Random(-5, 5) * 10`
4. 20% 概率进入“慢回蓝高蓝量”分支
5. 15% 概率进入“快回蓝低蓝量”分支
6. 小于下限时再做 clamp：
   - `mana_max >= 50`
   - `mana_charge_speed >= 10`
7. `15 + level * 6` 概率触发 `force_unshuffle`
8. 5% 概率 rare，额外 `cost + 65`
9. 将属性分为三组随机顺序结算：
   - `reload_time`
   - `fire_rate_wait`
   - `spread_degrees`
   - `speed_multiplier`
   - `deck_capacity`
   - `shuffle_deck_when_empty`
   - `actions_per_round`
10. 剩余 cost 大概率继续转成额外容量
11. 最终再做：
   - 容量 clamp
   - `reload_time >= 60` 时追加多重施法补偿逻辑

## 7.3 `better` 法杖流程的区别

`gun_procedural_better.lua` 与普通版相比，差异主要有：

- 属性概率表更窄
- `deck_capacity` 更偏向较高区间
- `reload_time` 区间更短
- `fire_rate_wait` / `spread` 波动更小
- `actions_per_round` 也更收敛
- 卡牌填充流程更接近“先定几张辅助牌，再堆主弹”的结构化模式

项目里已经将这部分拆成：

- `get_gun_data_better(...)`
- `wand_add_random_cards(..., procedure = "better")`

## 7.4 UI 法杖外观不是直接随机

程序法杖并不是“属性生成完后随便拿一张皮”。

脚本会把这些属性映射到 wand sprite 空间：

- `fire_rate_wait`
- `actions_per_round`
- `shuffle_deck_when_empty`
- `deck_capacity`
- `spread_degrees`
- `reload_time`

然后在所有 wand sprite 中选差值最小的一根。

因此即使两个模式法杖统计值接近，只要流程或属性表不同，最终 UI 外观也可能不同。

## 8. 法术生成逻辑差异

## 8.1 原先错误来源

本次问题的核心之一在于：

- 工具原先按 `spells.json` 顺序线性减权重实现 `GetRandomActionWithType`
- 这和游戏实际使用的参考概率表顺序不一致

导致结果是：

- 属性可能已经很接近
- 但内置法术完全错

## 8.2 正确的抽法更接近 `spells.h`

参考实现来自：

- `temp_noitaWandAtlas/src/spells.h`
- `temp_noitaWandAtlas/src/wandgen.h`

关键点：

- 每个 level / type 对应一张预先排序好的累计概率表
- 抽法更接近：
  - 取 `seededRandom`
  - 乘总权重得到 cutoff
  - 在累计表中定位命中项

而不是：

- 直接遍历 `spells.json`
- 按数据文件原顺序累减

## 8.3 这对噩梦入口法杖的影响非常直接

以种子 `10003796`、第一根入口法杖为例：

- 坐标修正到 `(703, -94)` 后
- 若仍按旧 `spells.json` 顺序抽取，工具会得到错误投射物
- 改成参考累计概率表后，才会稳定命中：
  - `SUMMON_HOLLOW_EGG x8`

## 8.4 当前项目修复结论

当前项目中：

- RNG 行为已朝参考实现对齐
- 法术抽取已改为使用参考累计概率表数据
- 这一步对入口法杖正确性是必要条件

## 9. 噩梦模式下已确认的入口法杖结果

以用户提供的种子 `10003796` 为例，当前已确认第一根入口法杖正确结果应为：

- `Shuffle = 是`
- `Spells/Cast = 2`
- `Cast delay = 0.20 s`
- `Rechrg. Time = 2.17 s`
- `Mana max = 310`
- `Mana chg. Spd = 109`
- `Capacity = 12`
- `Spread = -4.0°`
- 内置法术：
  - `SUMMON_HOLLOW_EGG` x8

在项目实现中，对应的是：

- 入口起点使用 `(703, -94)`
- 第一根按 `wand_level_02`
- 使用参考 RNG
- 使用参考法术累计概率表

## 10. Perk 差异

## 10.1 噩梦模式移除 `INVISIBILITY`

文件：

- `mods/nightmare/files/perk_list_appends.lua`

会从 perk 列表中删除：

- `INVISIBILITY`

因此工具在噩梦模式下也必须显式忽略该 perk。

项目中已有对应处理：

- `GameInfoProvider.updateConfig()`
- `nightmarePerks = ["INVISIBILITY"]`

## 10.2 噩梦模式存在入口 perk 流程

项目里噩梦模式除了入口法杖，还会生成入口 perk：

- `generateEntrancePerks(...)`

并且使用独立初始 index：

- `initialPerkIndex = 3`

这一点和普通模式直接进入常规圣山 perk 流程不同。

## 11. 玩家数值差异

`mods/nightmare/init.lua` 中，`OnPlayerSpawned()` 会提高玩家对多种伤害类型的承伤倍率。

### 11.1 实际改动

- `melee * 3`
- `projectile * 2`
- `explosion * 2`
- `electricity * 2`
- `fire * 2`
- `drill * 2`
- `slice * 2`
- `ice * 2`
- `radioactive * 2`
- `poison * 3`

### 11.2 结果

这意味着噩梦模式对玩家本体的容错显著更低，不是单纯“怪物更多”。

## 12. 生物群系敌人数值差异

`mods/nightmare/init.lua` 中，`OnWorldInitialized()` 会对多个 biome 执行：

- `BiomeSetValue(..., "game_enemy_hp_scale", hp_scale)`
- `BiomeSetValue(..., "game_enemy_attack_speed", attack_speed)`

### 12.1 被修改的 biome

代码里可以直接看到：

- `clouds`
- `coalmine`
- `coalmine_alt`
- `crypt`
- `desert`
- `excavationsite`
- `forest`
- `fungicave`
- `pyramid`
- `rainforest`
- `rainforest_open`
- `sandcave`
- `snowcastle`
- `snowcave`
- `snowcave_tunnel`
- `the_end`
- `the_sky`
- `vault`
- `vault_frozen`
- `wandcave`

### 12.2 示例

例如：

- `coalmine`：`hp_scale = 3`, `attack_speed = 0.5`
- `vault`：`hp_scale = 12.5`, `attack_speed = 0.5`
- `wandcave`：`hp_scale = 16`, `attack_speed = 0.5`
- `the_end` / `the_sky`：`hp_scale = 50`, `attack_speed = 0.1`

这些都属于普通模式没有的额外强化。

## 13. 噩梦模式额外像素场景处理

`mods/nightmare/init.lua` 中还会只执行一次：

- `LoadPixelScene("data/biome_impl/clean_entrance.png", ..., 128, 1535, ...)`
- `LoadPixelScene("data/biome_impl/clean_entrance.png", ..., 128, 4095, ...)`
- `LoadPixelScene("data/biome_impl/clean_entrance.png", ..., 128, 6655, ...)`
- `LoadPixelScene("data/biome_impl/clean_entrance.png", ..., 128, 10751, ...)`

并设置：

- `GameAddFlagRun("run_nightmare")`

这说明噩梦模式不仅换地图，还会在若干关键纵深位置额外铺设干净入口场景。

## 14. 药水逻辑差异

`mods/nightmare/files/potion_appends.lua` 重写了药水初始化。

### 14.1 主要逻辑

1. `SetRandomSeed(x, y)`
2. 50% 概率从 `materials_magic` 抽
3. 否则从 `materials_standard` 抽
4. 之后再检查 `VariableStorageComponent` 里是否存在强制指定的 `potion_material`
5. 最终 `AddMaterialInventoryMaterial(entity_id, potion_material, 1000)`

### 14.2 与普通模式的区别

仓库中噩梦脚本明确提升了魔法药液出现的权重，不能直接套普通模式药水预期。

## 15. 敌人附加 perk / 法杖差异

`mods/nightmare/files/director_helpers_appends.lua` 会改写敌人生成辅助逻辑。

## 15.1 敌人获得 perk

在 `entity_load_camera_bound(...)` 中：

- 有 `1 / 15` 概率令该次生成具备 `giveperk = true`

之后会：

- 从 `perk_list` 中筛选 `usable_by_enemies = true` 的 perk
- 随机给目标敌人挂一个

## 15.2 敌人获得法杖

同一逻辑里：

- 有 `1 / 20` 概率令该次生成具备 `givewand = true`

并根据敌人所在纵深估算 wand level：

- 太浅则至少 `2`
- `6 < level < 8` 时压到 `6`
- `level > 6` 时可直接转到 `10`

然后 50% 概率在：

- `wand_level_xx`
- `wand_unshuffle_xx`

之间二选一。

### 15.3 结果

噩梦模式下，同一敌人的实际威胁水平可能远高于普通模式原版配置。

## 16. Magic Numbers 差异

`mods/nightmare/files/magic_numbers.xml` 中除了替换 `BIOME_MAP`，还改了：

- `DAMAGE_BLOOD_AMOUNT_MIN="5"`
- `DAMAGE_BLOOD_AMOUNT_MAX="10"`
- `RAGDOLL_BLOOD_MULTIPLIER="1"`

这属于系统层面的全局数值覆盖。

## 17. 项目实现层面的必要分叉点

在本项目里，普通模式与噩梦模式至少必须分开处理以下模块：

- 世界地图
- 圣山/入口大厅坐标
- 入口 perk
- 入口法杖
- `wand_level_02_better`
- 噩梦特有 perk 忽略列表
- 法术累计概率表抽取

如果不分开，典型错误包括：

- 地图块位置不对
- 圣山层级不对
- 入口法杖模板不对
- 入口法杖属性不对
- 入口法杖内置法术不对
- 噩梦入口 perk 与实际不符

## 18. 当前项目内已落地的修复点

截至当前仓库状态，已完成的关键修复有：

### 18.1 地图

- `MapInfoProvider` 已支持 `isNightmare`
- 可切换到 `nightmareWorldMap.ts`

### 18.2 入口法杖位置

- 入口法杖坐标已从旧错误值修正到 `(703, -94)`

### 18.3 `wand_level_02_better`

- 已拆成独立 `better` 过程

### 18.4 RNG

- RNG 行为已向参考实现校正

### 18.5 法术抽取

- 不再只依赖 `spells.json` 顺序线性减权重
- 已切换到参考累计概率表思路

### 18.6 噩梦入口模板兼容

- 当前实现中按脚本实际写法兼容 `Random(1, opts)` 这一特殊情况

## 19. 对后续开发的建议

如果后续还要继续完善噩梦模式支持，建议保持以下原则：

### 19.1 普通模式和噩梦模式分模块处理

不要把噩梦模式当成普通模式的一个布尔修饰项，而应该至少拆开：

- 地图层
- 入口层
- perk 层
- 法杖层

### 19.2 入口法杖要优先以实机结果为准

对噩梦入口法杖来说，必须同时满足：

- 坐标准确
- RNG 准确
- 法术概率表顺序准确
- 模板选择行为准确

缺任一项都会偏。

### 19.3 所有“来自 mods/nightmare 的生成内容”都不要复用普通模式经验值

例如：

- 圣山结构
- 入口法杖
- 药水
- 敌人附加 perk / 法杖

都应优先回到 `mods/nightmare` 的脚本和像素场景本体。

## 20. 本次排查的关键结论摘要

以种子 `10003796` 为例，本次已确认：

1. 噩梦入口法杖是 `mods/nightmare` 专属内容，不属于普通模式逻辑。
2. 入口法杖实际生成坐标应按噩梦大厅像素场景标记重新推导，不能沿用普通模式或旧推导值。
3. 第一根法杖正确结果依赖：
   - 噩梦坐标 `(703, -94)`
   - 参考 RNG
   - 参考法术累计概率表
4. 第一根正确结果为：
   - `shuffle=1`
   - `actions_per_round=2`
   - `fire_rate_wait=12`
   - `reload_time=130`
   - `mana_max=310`
   - `mana_charge_speed=109`
   - `deck_capacity≈12.4`
   - `spread=-4`
   - `SUMMON_HOLLOW_EGG x8`

这一条已经足以证明：

- 噩梦模式的入口法杖、地图与法术抽取逻辑，必须与普通模式彻底分开处理。

## 21. 模块分叉矩阵

下表用于说明本项目哪些模块可以共用，哪些模块必须按普通/噩梦拆开。

| 模块 | 普通模式 | 噩梦模式 | 是否必须分叉 | 备注 |
| --- | --- | --- | --- | --- |
| 世界地图源 | 原版 world map | `nightmareWorldMap.ts` / `mods/nightmare/files/biome_map.lua` | 是 | 不能共用 |
| 圣山层级理解 | 普通主路径逻辑 | 噩梦主路径逻辑 | 是 | 层数、区域大小、兴趣点都不同 |
| 入口大厅脚本 | `data/scripts/biomes/mountain/mountain_hall.lua` | `mods/nightmare/.../mountain_hall.lua` | 是 | 噩梦多了入口法杖 |
| 入口 perk | 无噩梦入口 perk 特化 | 有 `generateEntrancePerks` + `initialPerkIndex=3` | 是 | 当前项目已分叉 |
| 入口法杖 | 不存在 | 存在 3 根专属法杖 | 是 | 普通模式不能复用 |
| 普通程序法杖 | `gun_procedural.lua` | 仍会用到 | 否 | 逻辑可共用 |
| `wand_level_02_better` | 很少单独关注 | 入口逻辑直接依赖 | 是 | 必须独立过程 |
| RNG 核心实现 | 统一 | 统一 | 否 | 但必须修到与游戏一致 |
| 法术池抽取 | 统一 | 统一 | 否 | 但必须使用参考累计表 |
| Perk 忽略列表 | 默认 | 忽略 `INVISIBILITY` | 是 | 当前项目已分叉 |
| 药水生成 | 原版 | `potion_appends.lua` | 是 | 权重不同 |
| 敌人生成辅助 | 原版 | `director_helpers_appends.lua` | 是 | 会给敌人加 perk / 法杖 |
| 玩家承伤 | 原版 | `OnPlayerSpawned()` 修改 | 是 | 系统数值不同 |
| biome 敌人数值 | 原版 | `BiomeSetValue(...)` 修改 | 是 | HP / 攻速不同 |

## 22. 入口法杖排查清单

如果以后某个种子的噩梦入口法杖再次对不上，建议按以下顺序排查。

### 22.1 第一层：位置是否正确

检查项：

- 当前入口大厅是否确实是噩梦模式地图
- `mountain_hall` 在噩梦地图中的 tile 是否仍是当前实现所用位置
- 像素场景 `hall.png` 中 `spawn_all_shopitems` 颜色标记坐标是否变化
- 生成起点是否仍应为 `(703, -94)`
- 后两根是否仍按 `44` 间距排列

如果这一层错了，后续 RNG 和法术池都会整体偏掉。

### 22.2 第二层：模板是否正确

检查项：

- `mods/nightmare/data/scripts/biomes/mountain/mountain_hall.lua` 是否仍写成 `Random(1, opts)`
- 工具是否仍按“固定第一项”来模拟
- 如果未来确认实机不是固定第一项，而是真正随机，则这里需要单独回滚重做

如果这一层错了，通常会表现为：

- 法杖大体属性有些接近
- 但 `shuffle`、容量、法杖类别明显不对

### 22.3 第三层：RNG 是否正确

检查项：

- `SetWorldSeed`
- `SetRandomSeed`
- `Random`
- `RandomDistribution`
- `RandomDistributionf`
- `SeededRandom`

是否仍与参考实现一致。

如果这一层错了，通常会表现为：

- 基础属性整体漂移
- mana / reload / cast delay / actions_per_round 经常一起偏

### 22.4 第四层：法术表是否正确

检查项：

- 是否仍使用 `spellProbTables.json`
- 该表是否仍对应参考 `spells.h`
- `GetRandomActionWithType` 是否仍按参考累计概率表抽取
- `unlockedSpells` 是否已正确初始化

如果这一层错了，通常会表现为：

- 法杖属性已经完全正确
- 但内部法术与实机完全不同

### 22.5 第五层：`better` 流程是否被误用

检查项：

- 当前模板到底是 `wand_level_02` 还是 `wand_level_02_better`
- `provide(..., procedure)` 是否传对
- `better` 流程里卡牌填充顺序是否被改坏

如果这一层错了，通常会表现为：

- 法杖总体风格不对
- 抽牌法术 / 修饰法术比例异常

## 23. 结论可信度分级

为了避免以后混淆，下面把本文中的结论分成三类。

### 23.1 A 类：仓库脚本可直接证明

这些内容可以直接从仓库文件中读取，不依赖推测：

- 噩梦模式替换 `BIOME_MAP`
- 移除 `INVISIBILITY`
- 玩家承伤倍率修改
- biome HP / 攻速修改
- 药水脚本改写
- 敌人附加 perk / 法杖逻辑
- 入口大厅有 `spawn_all_shopitems`
- 候选模板集合内容
- `wand_level_02_better` 使用 `gun_procedural_better.lua`

### 23.2 B 类：结合实机结果与代码实现确认

这些内容是本次排查中已经较强确认的结论：

- 噩梦入口法杖起点使用 `(703, -94)`
- 第一根法杖正确结果为 `SUMMON_HOLLOW_EGG x8`
- 法术抽取必须使用参考累计概率表
- `wand_level_02_better` 不能按普通流程对待

### 23.3 C 类：当前实现层面的兼容假设

这些内容当前是“为了贴合实机结果而采取的实现策略”，后续若有更强实机证据，允许再修正：

- `Random(1, opts)` 在噩梦入口大厅中应模拟为固定选第一个模板
- 噩梦主路径按 5 层圣山来处理的描述

这两项目前与仓库脚本及用户实机样本是相容的，但仍建议在更多实机样本下继续验证。

## 24. 后续文档维护建议

如果后面还要继续扩写这份文档，建议按以下顺序补充，而不是无序堆结论。

### 24.1 先补“实机样本表”

建议追加一张表，记录：

- 种子
- 模式
- 入口法杖 1/2/3
- 属性摘要
- 内置法术摘要
- 是否与工具一致

这样以后改 RNG 或法术池时，可以快速回归。

### 24.2 再补“地图 tile 对照表”

建议把普通/噩梦模式下这些关键区域的 tile 坐标列出来：

- `mountain_hall`
- 后续圣山
- 关键主路径 biome
- 关键支线 biome

### 24.3 最后补“接口级维护说明”

建议未来在文档里单独增加一个章节，明确这些接口的职责：

- `GameInfoProvider.updateConfig()`
- `MapInfoProvider.setNightmareMode(...)`
- `EntranceWandInfoProvider.provide()`
- `WandInfoProvider.provide(...)`
- `genRandom(...)`

这样下一次改动时，就不会把“地图切换”、“入口法杖生成”和“通用法杖生成”混在一起改坏。
